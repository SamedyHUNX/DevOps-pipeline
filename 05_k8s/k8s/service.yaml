apiVersion: v1
kind: Service
metadata:
  name: kubernetes-demo-service
  labels:
    app: kubernetes-demo
spec:
  selector:
    app: kubernetes-demo
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: NodePort
